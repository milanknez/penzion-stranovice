RewriteEngine On
RewriteBase /

# Skip existing files and directories
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Send everything else to router.php
RewriteRule ^(.*)$ router.php [L,QSA]
